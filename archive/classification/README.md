# Classification Archive

This folder contains earlier molecule classification notebooks and generated classified molecule outputs that are no longer part of the active BrainRoute database platform build.

The current platform application lives in `brainroute-app`.
